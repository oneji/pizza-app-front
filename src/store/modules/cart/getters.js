export default {
    getCurrency(state) {
        return state.currency;
    },

    getItems(state) {
        return state.items;
    },

    getSnackbar(state) {
        return state.snackbar;
    }
}